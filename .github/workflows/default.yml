name: default

on:
  push:
  pull_request:

permissions:
  contents: read
  pull-requests: write
  repository-projects: write  

jobs:
  default:
    runs-on: ubuntu-latest
    steps:
      - uses: rapsealk/ghaflow@dab0f2800c21435de8f2833770c7d26bf9957fd9
