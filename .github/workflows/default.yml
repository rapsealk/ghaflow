name: default

on:
  push:

permissions:
  contents: read
  pull-requests: write
  repository-projects: write  

jobs:
  default:
    runs-on: ubuntu-latest
    steps:
      - uses: rapsealk/ghaflow@83e61d291ef5d81d5d851c5784ee23f39cd44af8
